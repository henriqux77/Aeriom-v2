/* AERION MIND MAP V2 — emergency safe mode
 * This module is intentionally inert while the mental-map system is rebuilt.
 * It must never alter the campaign DOM or register observers on load.
 */
(() => {
  "use strict";
  window.AERION_MIND_MAP_V2_DISABLED = true;
})();